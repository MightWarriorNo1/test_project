# Optional: set when deploying frontend to Vercel so it talks to your API.
# Example: https://your-api.fly.io  or  https://api.yourdomain.com
# Leave unset for local dev (uses /api proxy to http://localhost:8000).
# VITE_API_URL=https://your-backend-url.com
